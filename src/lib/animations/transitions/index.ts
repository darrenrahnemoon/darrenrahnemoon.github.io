import Fade from './Fade.vue';
import SlideFade from './SlideFade.vue';

export default {
    Fade,
    SlideFade,
};

export enum TransitionVariant {
    Fade      = 'fade',
    SlideFade = 'slideFade',
}
